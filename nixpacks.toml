[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo_mysql", "composer"]

[phases.install]
cmds = ["composer install --ignore-platform-reqs --no-dev"]

[start]
cmd = "php -S 0.0.0.0:$PORT"